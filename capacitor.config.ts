import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bugkheiyb.sugarapple',
  appName: 'Sugar Apple - บักเขียบ',
  webDir: 'www/browser'
};

export default config;
